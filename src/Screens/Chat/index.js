import React from "react";
import { View, Text } from "react-native";

const Chat = (props) => {
    return (
        <View>
            <Text>Chatting</Text>
        </View>
    );
};

export default Chat;